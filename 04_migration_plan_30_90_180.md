# 04 Migration Plan (30 / 90 / 180 days)

## 0-30 天（P0：安全、隔离、稳定）

1. 密钥治理与配置硬化
   - 强制 `JWT_SECRET` 启动校验（替代默认值）
   - 将 `.env` 中密钥迁出仓库并轮换
   - 证据来源：`go-core/main.go:320-326`, `.env:7-9`
2. 队列可靠性最低闭环
   - 为 worker 增加失败重试计数、失败回写、DLQ
   - 引入 pending reclaim（XAUTOCLAIM）
   - 证据来源：`python-worker/main.py:1561-1649`
3. 观测基础
   - 接入 request_id、结构化日志字段（user_id/photo_id/job_id）
   - 新增关键链路报警（上传失败率、队列积压、worker 错误率）

## 31-90 天（P1：SaaS 核心能力）

1. 多租户模型落地（组织级）
   - 新增 tenant/org/member 表与迁移
   - API/DB query 全链路 tenant filter
   - 对象命名切换到 tenant-aware 路径
2. 计费计量体系
   - 采集处理次数、AI 调用、存储使用量变更事件
   - 建立按租户/套餐汇总视图和限额策略
3. 安全提升
   - internal API 从静态 secret 升级为 mTLS 或签名票据
   - 队列不再传递明文 api_key，改配置引用

## 91-180 天（P2：规模化与治理）

1. 可扩展与弹性
   - worker 水平扩展策略（分队列并发与资源隔离）
   - 热点任务限流与优先级队列
2. 数据治理与合规
   - 生命周期策略（归档、删除、审计留存）
   - 备份恢复演练（季度）并固化 RPO/RTO
3. 交付工程化
   - CI/CD 分环境发布、灰度/回滚、变更审批与审计

## 里程碑验收（摘要）

- M1（30天）：
  - 不再存在默认 JWT 弱值；队列失败有可见/可回放闭环；关键告警上线
- M2（90天）：
  - tenant_id 全链路贯穿；计量数据可对账；内部通信安全升级完成
- M3（180天）：
  - 支持规模化扩展；合规治理体系与发布体系稳定运行
